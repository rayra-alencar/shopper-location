mutation updateOrderFormShipping(
  $orderFormId: ID
  $address: AddressInput!
) @context(provider: "vtex.checkout-graphql") {
  updateOrderFormShipping(orderFormId: $orderFormId, input: $address) {
    id
  }
}
